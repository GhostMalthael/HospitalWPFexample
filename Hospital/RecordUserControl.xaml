<UserControl x:Class="Hospital.RecordUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hospital"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="500" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock x:Name="recordTimeTextBlock" Text="Дата и время приема: " Margin="10,10,262,64"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="recordNameDoctor" Text="Прием у врача: " Margin="10,10,14,67"/>
            <Button Grid.Column="1" x:Name="buttonCanselSignUp" Content="Отменить запись" Margin="62,37,62,37" Click="buttonCanselSignUp_Click" />
        </Grid>
    </Grid>
</UserControl>
